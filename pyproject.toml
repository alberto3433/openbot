[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orderbot"
version = "0.1.6"
description = "Multi-tenant restaurant ordering chatbot"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "alembic>=1.17.0",
    "fastapi>=0.123.0",
    "inflect>=7.0.0",
    "openai>=2.9.0",
    "pydantic>=2.10.0",
    "python-dotenv>=1.0.0",
    "slowapi>=0.1.9",
    "SQLAlchemy>=2.0.0",
    "stripe>=8.0.0",
    "twilio>=9.0.0",
    "uvicorn>=0.38.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
    "pytest-xdist>=3.5.0",
    "httpx>=0.27.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["orderbot*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-n 4 --timeout=120 --ignore=tests/chaos_monkey/generated"
